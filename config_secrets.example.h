// Copy this file to config_secrets.h (same folder) and fill in real values.
// config_secrets.h is gitignored — it will never be committed.
// Get a real device token from an authorized hospital/organizer account via
// POST /api/v1/devices/provision — never invent one or reuse this example.

#define WIFI_SSID   "your-real-wifi-ssid"
#define WIFI_PASS   "your-real-wifi-password"
#define DEVICE_TOKEN  "token-returned-by-devices-provision-endpoint"
